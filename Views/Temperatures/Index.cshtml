@model IEnumerable<WebApplication10_Nov10.Models.TemperatureModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Month)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Month.MonthName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TemperatureId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TemperatureId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TemperatureId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
